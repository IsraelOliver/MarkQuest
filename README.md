# MarkQuest

Monorepo inicial do MarkQuest com frontend e backend em TypeScript.

## Projetos

- `./` Frontend (React + Vite)
- `./backend` API (Fastify + TypeScript)

## Backend (API)

```bash
cd backend
cp .env.example .env
npm install
npm run dev
```
